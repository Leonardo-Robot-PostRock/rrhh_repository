{
  "name": "form-validator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/validator.js --bundle --outfile=dist/bundle.js --minify --sourcemap && tailwindcss -i src/styles/tailwind.css -o dist/styles.css --minify",
    "build:js": "esbuild src/validator.js --bundle --outfile=dist/bundle.js --minify --sourcemap",
    "build:css": "tailwindcss -i src/styles/tailwind.css -o dist/styles.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "esbuild": "^0.18.18",
    "tailwindcss": "^4.1.18"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1"
  }
}